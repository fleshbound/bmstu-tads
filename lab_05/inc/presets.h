#ifndef __PRESETS_H__
#define __PRESETS_H__

#define MIN_MENU_KEY 0
#define MAX_MENU_KEY 9
#define MIN_DEMO_VALUE -1000
#define MAX_DEMO_VALUE 1000

#define MIN(x, y) x < y ? x : y;

#define MAX(x, y) x > y ? x : y;

#define ABS(x) x > 0 ? x : -x;

#endif  // __PRESETS_H__
